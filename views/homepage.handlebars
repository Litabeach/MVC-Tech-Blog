{{#each blogs as |blog| }}
<div class="card text-white bg-dark mb-3" style="max-width: 50rem;">
   <div class="card-header">{{blog.title}}  <p>Created by {{user.name}} on {{format_date blog.createdAt}}</p></div>
  <div class="card-body">
    <p class="card-text">{{blog.description}}.</p>
  </div>
</div>
{{/each}}  

{{!-- {{#each blogs as |blog|}} --}}
{{!-- <div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{blog.title}} {{user.name}} {{blog.createdAt}}</h2>
  </div>
  <div class="col-md-6">
    <p>{{blog.description}}</p>
  </div>
</div>
{{/each}} --}} 

{{!-- <div class="home">
  <h1 style="color:rgba(94, 39, 39, 0.743); font-weight:bold"> Current Blogs </h1>
</div> --}}

{{!-- {{#if loggedIn}} --}}
{{!-- {{#each blogs}}
<a href="/api/blogs/{{id}}" class="blogLink">
<div class="post">
<p>{{format_date createdAt}}</p>
<h2>{{title}}</h2>
<p>{{description}}</p>
</div>
</a>
{{/each}} --}}
{{!-- {{/if}} --}}

{{!-- {{#unless loggedIn}} --}}
{{!-- {{#each blogs}}
<div class="post">
<h2>{{title}}</h2>
<p>{{content}}</p>
</div>
{{/each}}
{{/unless}} --}}